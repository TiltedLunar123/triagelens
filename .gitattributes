# Normalize line endings to LF in the repository for cross-platform consistency.
* text=auto eol=lf

# Treat common binary assets as binary so they are never normalized.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
